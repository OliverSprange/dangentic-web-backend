{
  "name": "dangentic-keystone-cms",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "keystone build",
    "dev": "keystone dev",
    "postinstall": "keystone postinstall",
    "generate": "keystone prisma migrate dev",
    "start": "keystone start --with-migrations",
    "deploy": "keystone build && keystone prisma migrate deploy",
    "start-prod": "keystone start"
  },
  "dependencies": {
    "@keystone-6/core": "^6.4.1",
    "@keystone-6/fields-document": "^9.1.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
